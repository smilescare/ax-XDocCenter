<?xml version="1.0" encoding="UTF-8"?>
<site-conf xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="http://ofbiz.apache.org/dtds/site-conf.xsd">
    <!-- The controller elements that are common to all OFBiz components
         can be found in the following xml file. A component can override the
         elements found in the common-controller.xml file. -->
    <include location="component://common/webcommon/WEB-INF/common-controller.xml"/>
    <include location="component://helpcenter/webapp/helpcenter/WEB-INF/cms-controller.xml"/>

    <description>helpcenter Component Site Configuration File</description>

    <!-- Events to run on every request before security (chains exempt) -->
    <!--
    <preprocessor>
    </preprocessor>
    -->
    <!-- Events to run on every request after all other processing (chains exempt) -->
    <!--
    <postprocessor>
        <event name="test" type="java" path="org.ofbiz.webapp.event.TestEvent" invoke="test"/>
    </postprocessor>
    -->

    <!-- Request Mappings -->
    <!-- default request; call into CMS -->
    <default-request request-uri="cms"/>
    
    <!-- General Request Mappings -->
    <request-map uri="cms">
    	<security https="true" auth="false" />
        <event type="java" path="org.ofbiz.content.cms.CmsEvents" invoke="cms"/>
        <response name="success" type="none"/>
        <response name="error" type="view" value="error"/>
    </request-map>
    
    <request-map uri="main"><security https="true" auth="true"/><response name="success" type="view" value="main"/></request-map>
    
    <request-map uri="builder">
		<security https="true" auth="true" />
		<response name="success" type="view" value="builder" />
	</request-map>
    <request-map uri="defaultcontent">
        <security https="true" auth="false"/>
        <response name="success" type="view" value="defaultcontent"/>
    </request-map>
    <request-map uri="viewcontent">
        <security https="true" auth="false"/>
        <response name="success" type="view" value="viewcontent"/>
    </request-map>
    <request-map uri="img">
        <security auth="false" https="false"/>
        <event type="java" path="org.ofbiz.content.data.DataEvents" invoke="serveImage"/>
        <response name="success" type="none"/>
        <response name="error" type="view" value="main"/>
    </request-map>
    
    <!-- View Mappings -->
    <view-map name="main" type="screen" page="component://helpcenter/widget/helpcenterScreens.xml#main"/>
    <view-map name="builder" type="screen" page="component://helpcenter/widget/PageBuilderScreens.xml#builder"/>
    
    <view-map name="defaultcontent" type="screen" page="component://helpcenter/widget/ContentScreens.xml#defaultcontent"/>
    <view-map name="viewcontent" type="screen" page="component://helpcenter/widget/ContentScreens.xml#viewcontent"/>
</site-conf>